export default function LoginPage() {
  return (
    <section className="mx-auto flex min-h-screen w-full max-w-3xl flex-col justify-center px-6 py-16">
      <p className="text-sm font-medium uppercase tracking-wide text-slate-500">
        Rota publica
      </p>
      <h1 className="mt-3 text-3xl font-semibold text-slate-950">Login</h1>
      <p className="mt-4 text-slate-600">
        Placeholder da rota /login. O formulario sera implementado em outra
        branch.
      </p>
    </section>
  );
}
